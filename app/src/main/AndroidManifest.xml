<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.winningreen">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_wg_xhdpi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_wg_xhdpi"
        android:supportsRtl="true"
        android:theme="@style/Theme.WinninGreen">
        <activity android:name=".DashboardActivity" />
        <activity android:name=".TokenGenerationActivity" />
        <activity android:name=".ThingIdActivity" />
        <activity android:name=".CredentialsActivity" />

        <activity
            android:name=".FirstScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>
